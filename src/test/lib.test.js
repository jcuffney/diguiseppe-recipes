const assert = require('assert');

describe('lib', () => {
  describe('readFile', () => {
    it('throws when no file at path', () => {
      assert.equal(true, false);
    });

    it('returns JSON when file exists', () => {
      assert.equal(true, false);
    });
  });
});
